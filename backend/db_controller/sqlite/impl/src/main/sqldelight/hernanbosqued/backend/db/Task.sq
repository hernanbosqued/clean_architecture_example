selectByUserId:
SELECT * FROM taskEntity WHERE user_id = ?;



insert:
INSERT INTO taskEntity (user_id, name, description, priority) VALUES (?, ?, ?, ?);

remove:
DELETE FROM taskEntity WHERE id = ?;
